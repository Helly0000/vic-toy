# 开发机制：一套自洽的世界 + 自主决策的智能体，玩家插入其中

> 这篇回答的不是"加什么功能"，而是**我们到底在建什么**。
> 它给前面所有实验一个统一的位置：世界是物理层，智能体是决策层，玩家是其中一个智能体。

---

## 一、一句话定义

> **我们不是在做一个模拟器，也不是在做一个游戏，而是在做「一个自洽的世界 + 若干自主决策者」。
> 玩家不是世界的操控者，而是其中一个决策者——只是他的决策来自一个人类。**

这句话决定了三件事：

1. 世界必须有**独立于玩家的规律**：你不操作，世界照样演化、照样有人赢有人输；
2. 每个决策者（含玩家）**只能通过同一套接口**影响世界，没有 GM 通道；
3. 乐趣来自**多个目标不同的决策者之间的张力**，而不是"操作反馈循环"。

---

## 二、三层结构（每层只做一件事）

```
┌─────────────────────────────────────────────────────────────┐
│  L3  表现层（浏览器）                                         │
│      渲染 / 面板 / 玩家输入采集                                │
│      唯一职责：把世界画出来，把人的意图转成"意图"                │
└───────────────────────────┬─────────────────────────────────┘
                            │ 意图（不是状态改动）
┌───────────────────────────▼─────────────────────────────────┐
│  L2  决策层（智能体）                                         │
│      玩家智能体（人） / AI 智能体（N 个）                       │
│      输入：自己看得到的世界快照（不是全部）                      │
│      输出：命令（同一套）                                      │
│      唯一职责：决定"我要做什么"                                 │
└───────────────────────────┬─────────────────────────────────┘
                            │ pushCommand(...)  ← 唯一入口
┌───────────────────────────▼─────────────────────────────────┐
│  L1  世界层（物理）                                           │
│      world 的 47 个 TypedArray + tick() 的十步清算             │
│      唯一职责：执行命令、演化状态、产生后果                      │
│      不许有决策逻辑；不许知道谁是玩家                            │
└─────────────────────────────────────────────────────────────┘
```

**边界纪律（三者缺一不可）**：

| 规则 | 为什么 |
|---|---|
| **只有 `tick()` 能改 world** | 世界状态只有一个写入口，才可回放、可测试、可存档 |
| **决策者只能推命令，不能改状态** | 玩家和 AI 走完全相同的路径，公平性由架构保证而不是靠自觉 |
| **决策者只能看到自己那份快照** | 不完美信息是所有"心智博弈"的前提；全知的 AI 会让世界退化成一个可解的谜题 |

---

## 三、现状对照：我们已经站在这个结构上了

| 层 | 现状 | 差什么 |
|---|---|---|
| L1 世界 | ✅ 已经有：47 个预分配数组、`tick()` 十步清算、确定性 RNG | 需要把"要素上限"接进来（见冷战设计文档） |
| L2 决策 | ⚠️ **只有一个决策者，而且长在世界里**：`autoInvest` 逻辑写在 `tick()` 第 7 步内部 | 把它抽出来，变成 L2 的第一个智能体 |
| L2 命令接口 | ✅ 已经有：`pushCommand` / `applyCommands`（UI 与张力测试台共用） | 命令类型太少（只有建造/赈灾/开关） |
| L3 表现 | ✅ 已经有：渲染 + 面板 + 建造/赈灾按钮 | 面板要能显示"我看得到什么"（情报范围） |

**关键的一步其实很小**：把 `tick()` 里那段自动投资（第 7 步，`w.invest[p] += revenue * INVEST_RATE` 后面那一整块）
搬到一个函数 `aiInvest(world, countryId)` 里，让它**推命令**而不是直接改状态。

做完这一步，`stratGeoAware()`（现在躺在 `test/tension.js` 里）就立刻变成"一个真正的 AI 国家"。
**这不是重构，是把已经写好的东西放到正确的位置。**

---

## 四、智能体的五个要素（"自主"是可验证的，不是形容词）

一个智能体 = 下面五件事的**显式声明**。任何一项缺失，它就不是自主的，而是脚本。

| 要素 | 含义 | 反例（退化成脚本） |
|---|---|---|
| **① 拥有的状态** | 它自己记得什么（国库预算、在建计划、对别人的判断） | 每次都从 world 现算一遍，没有记忆 |
| **② 目标与权重** | 它要什么，多项冲突时怎么折算 | 只有一个硬目标，或权重写死不可配置 |
| **③ 可动用的手段** | 它在这局里有权限做什么 | 有"只有 AI 能做"的手段（作弊） |
| **④ 看得到的范围** | 它的信息边界（自己的省 + 邻国 + 已知价格） | 直接读全世界的精确数字 |
| **⑤ 决策规则** | 从 ①②③④ 推出命令的机制 | 见下 |

### 四种决策规则（按成本/天花板排序）

| 机制 | 成本 | 上限 | 适合 |
|---|---|---|---|
| **规则表**（if-then） | 极低 | 低 | 第一期：`stratGeoAware` 就是这个 |
| **效用打分**（utility ai） | 低 | 中高 | **推荐起点**：把"多目标冲突"表达成加权分，天然可调 |
| **目标规划**（goal → plan） | 中 | 高 | 有能力做多步计划时（"三年内让粮食自给"） |
| **学习**（自对弈调参） | 高 | 最高 | 最后再说；没有前三种打底，学出来的东西不可解释 |

**建议：从效用打分开始。** 理由很实在——它让"AI 的性格"变成一个可以调、可以看的权重向量，
于是**调参本身就是玩法设计**：把"好战"权重调高，世界就该更动荡。

### 三种时间尺度（防止所有决策挤在一个 tick 里）

| 尺度 | 频率 | 决策例子 |
|---|---|---|
| 快 | 每月 | 建造、赈灾、储备动用 |
| 中 | 每年 | 税率、贸易协定、阵营倾斜 |
| 慢 | 每 5~10 年 | 技术路线、制度选择、战略目标重设 |

慢决策要**真的慢**：如果 AI 每个 tick 都能改战略，它就没有"承诺"，而承诺是博弈的地基。

---

## 五、谁定什么（避免系统打架）

| 东西 | 谁定 | 说明 |
|---|---|---|
| 世界的规律（要素、配方、价格机制） | **开发时设计，运行时不可改** | 这是"物理定律"，玩家和 AI 都不能碰 |
| 世界资源的初始分布 | **地图**（真实地球线或随机生成） | 涌现的地基 |
| 每个决策者的目标权重 | 开局设定 + 长期演化 | 玩家的权重由人给；AI 的权重可被"制度/文化"影响 |
| 每月的具体命令 | 各决策者自己 | 只有这一层是"自由意志" |
| 意外事件 | **世界按规律产生**，不是脚本 | 歉收是产出公式的后果，不是"第 60 回合触发" |

**这张表的意义**：它规定了"哪里可以写死、哪里必须涌现"。
写死的只有物理定律；除此之外都要能从状态推出来。这就是"不依赖手写精巧事件"的架构版本。

---

## 六、怎么证伪（"自主"必须可测量）

这是 vic-toy 一直在做的事：**把设计问题变成可复跑的数字**。智能体层同样要立四条度量：

| 度量 | 定义 | 不达标意味着 |
|---|---|---|
| **性格可分** | 只改目标权重，能否产出统计上不同的世界 | 权重是装饰，AI 其实是同一个规则表 |
| **基线压制** | AI 对比随机策略的胜率、以及 `tension.js` 里"不作为"的差距 | AI 太弱 → 世界没有压力，玩家躺赢 |
| **非作弊** | AI 会不会利用玩家没有的手段（逐条审计命令类型与信息读取范围） | 出现"只有 AI 能用"的捷径 → 世界不自洽 |
| **有世界感** | 玩家完全不操作时，世界仍然演化、仍然有国家兴衰 | 没有玩家就没戏 → 我们做的其实是解谜游戏 |

第四条最容易被忽略，但它直接对应你说的"我作为玩家插入进去"：
**如果一个不操作的世界是静止的，那玩家就不是"插入"，而是"驱动"。**

---

## 七、最大的风险：把 AI 做成全知最优解

这是这类设计的头号杀手。全知最优的 AI 会同时干掉三样东西：

1. **选择空间**——如果存在唯一最优解，玩家的探索就没有意义；
2. **世界感**——如果 AI 从不犯错，历史就不像历史；
3. **可玩性**——玩家会去抄 AI 的作业，而不是形成自己的打法。

对抗它的三个手段，全部来自上面那张表：

- **信息边界（④）**：AI 只看得到自己那份快照，允许它判断错误；
- **目标多元（②）**：给它互相冲突的目标（安全 vs 增长 vs 意识形态），让它必须取舍；
- **承诺机制（时间尺度）**：慢决策不可随时反悔，于是它会"选错并承担"。

> **一句话**：智能体层是**世界的镜子**，不是世界的**求解器**。

---

## 八、和已经做完的实验怎么接上

| 已有产物 | 在这套架构里的位置 |
|---|---|
| `sim.js` 的 `tick()` 十步清算 | **L1 世界**的物理定律 |
| `pushCommand` / `applyCommands` | **L2 → L1 的唯一接口**（玩家与 AI 共用） |
| `test/tension.js` 里的 `strat*` 函数 | **L2 的第一批智能体**（现在只跑在玩家国上） |
| `test/earth1945.js` + 要素配方 | **地图与物理定律**的真实版（决定谁必须买、谁能卖） |
| `detectFamine` 的"量的冲击" | **世界按规律产生的意外**（不是脚本事件） |
| `test/factor-sweep.js` 的史实断言 | **物理定律的验收标准**（"像不像那个世界"） |

**下一步只有一件小事，但它决定后面所有事**：
把 `tick()` 里的自动投资搬进 `aiInvest(world, countryId)`，让全世界 8 国各跑一个智能体。
那一刻起，玩家就不再是"世界的推动者"，而是"插入其中的人"。
