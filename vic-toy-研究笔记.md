# vic-toy 研究笔记

> 研究时间：随本次会话；方法 = 通读 3500 行源码 + 跑全部自带测试 + 用 Edge（CDP）实测浏览器 + 自写三个独立探针。
> 结论一句话：**代码质量明显高于"玩具"这个词**，架构纪律真实且被测试守住；但模拟在约 100 年（1200 tick）后进入完全冻结的稳态，地图失去全部区分度——这比 README 承认的更早、更彻底。

---

## 一、这是什么

`蒸汽与账本 · 1836`：浏览器里的大战略**经济模拟玩具**（维多利亚 3 的戏仿）。纯 JavaScript 经典 `<script>` + Canvas 2D，零依赖 / 零构建 / 零服务器，双击 `index.html` 即玩。

| 项 | 事实 |
|---|---|
| 代码量 | 3500 行（js 2188 / css 448 / html 103 / test 361 / README） |
| 世界 | seed 8888：214 省 / 8 国 / 2 块陆地（主大陆 179 省 + 南部小岛） |
| 模拟 | 3 阶层 × 5 商品 × 8 个**封闭**国市场，1 tick = 1 月 |
| 界面 | 顶栏时钟 + 左省账本 + 右市场/国力排行 + 底大事记 + 5 种地图模式 |
| 版本控制 | **没有 git**，没有 package.json，没有任何构建配置 |
| 实测生成耗时 | 浏览器内 **451 ms**（Node 内 mapgen 333 ms） |

---

## 二、实测结果（全部是本次跑出来的，不是引用 README）

### 1. 自带回归测试：44 项全绿

```
node test/check.js  →  全部通过：44 项，耗时 1.17 s，退出码 0
```

3 个种子 × (地图生成 7 项 + 拾取 2 项 + 模拟 6 项) + 经济标定 1 项。断言质量不错：陆地每个连通域都必须有省份（"南极洲 bug"的回归）、海洋零误判拾取、收支比区间、单 tick 耗时。

### 2. 浏览器实测（Edge 153 headless + CDP，1680×1020）

| 项目 | 结果 |
|---|---|
| 控制台报错 | **0 条**（唯一日志是世界生成耗时） |
| 加载遮罩 | 正常消失，面板全部填充（市场 5 行、排行 8 行） |
| 帧率 政治模式 24× | **60.2 fps**，p50 16.7ms，worst 16.8ms，无 >33ms 掉帧 |
| 帧率 工业/不安定/财富（数据模式）24× | **60.3 fps**，3 秒内 18 次整层重建，仍无掉帧 |
| 帧率 政治模式**暂停** | 60.4 fps —— 证实 `draw()` 脏标记真的在生效（README 坑 #12 已彻底修好） |
| 国家是否连片 | **8 国全部单连通**，零碎片；零无主陆地 |
| 国名标注锚点 | **8/8 落在本国领土内**（邻域本国占比 93~100%） |

### 3. 150 年长跑（项目自己的测试只跑 25 年）

| tick | 年份 | 总人口 | 均财富 | 均收支 | 均不满 | 均建筑 | 满级省 | 国度 | 国库 |
|---|---|---|---|---|---|---|---|---|---|
| 0 | 1836 | 35.9M | 1.00 | **0.82** | 0.234 | 15.9 | 0/214 | 1.2M | 0.3M |
| 60 | 1841 | 38.4M | 1.35 | 1.124 | 0.059 | 22.8 | 0/214 | 1.8M | 8M |
| 300 | 1861 | 47.6M | 2.51 | 1.055 | 0.057 | 43.9 | 0/214 | 3.7M | 67M |
| 600 | 1886 | 54.6M | 3.23 | 1.029 | 0.050 | 62.6 | 12/214 | 5.3M | 185M |
| **1200** | **1936** | 56.8M | 3.42 | 1.000 | 0.043 | **68.4** | **207/214** | 5.8M | 462M |
| 1800 | 1986 | 56.9M | 3.42 | 1.000 | 0.043 | 68.4 | 207/214 | 5.8M | 739M |

**相变点在 1200 tick 附近（约 100 年）**：此后

- 207/214 个省全部 5 类建筑**全部满级**（MAX_LEVEL=14 → 70 级）；
- 人口、GDP、均财富、不满**全部冻结**（0.043 恒定），只剩国库以固定速率线性灌水（每年约 +2.7M），因为**没有花钱的地方**；
- 地图模式集体失灵：建筑级数 p05=p50=p92=70（**92 分位/p50 = 1.00**），底层财富 p05=2.37 / p92=3.94，但 92 分位归一化把它们压成同一段色带；不满 p50=0，只有少数省还亮着。
- 换句话说：**115 岁以后这块地图不再传递任何信息**，而 README 只承认"没有玩家操作"，没承认"没有玩家操作时 100 年就走到终点"。

**开局是"先通缩再自愈"**：tick 0 平均收支比 0.82、均不满 0.234、最差省底层收支 0.26（全国近四成阶层赤字），到 tick 60 才变成 1.12。README 的"开局总产出 ≈ 总消费"说的是产出侧标定，**没提开局这一波全民赤字跑**。是设计还是副作用，值得写清楚。

### 4. 性能

长跑 1800 tick 后单 tick 仍 **0.030 ms**（比 README 开局基线 0.14ms 还快——因为建筑全满级后投资分支不再触发）。tick 循环零分配这条纪律确实体现在数字上。

---

## 三、发现的问题

### 已确认的 bug：左侧面板"省份/国家数量"永远显示 `—`

- `js/ui.js:61-62` 在 `init()` 里给 `#hint-provinces` / `#hint-countries` 写了 214 / 8；
- 但 `js/main.js:66-69` 紧接着调 `VIC.ui.showCountryOverview()`，而 `js/ui.js:254` 执行 `el.selBody.innerHTML = html` —— **整个 `sel-body` 被换掉，那对 `<b>` 元素直接从 DOM 移除**；
- 现象：截图里"这片大陆上有 **—** 个省份、**—** 个国家"。

**修法**（二选一）：
1. `showCountryOverview` / `showProvince` 只重写 `selBody` 的内层容器，别碰外层；
2. 或把这两个数字改由 `showCountryOverview` 自己负责显示（它本来就在算"国力排名 1/8"）。

这是 render.js / ui.js **没有自动化测试**的直接后果——README 已知遗留 #4 说对了，这条就是第一个受害者。

### 文档与代码不一致

| README 说法 | 实际情况 |
|---|---|
| `sim.js` 有"三个入口：tick() / snapshot() / applyCommand()" | 实际导出 `createWorld/tick/countryRows/marketRows/provinceRows`，**`snapshot()` 和 `applyCommand()` 不存在**（是玩家操作那一步的规划遗留） |
| 顶栏"人口" | 显示的是**当前国家**人口，不是世界总人口（实测 1843 年是 9.04M，而世界总量约 40M+） |
| "214 省" | 是 Voronoi 补种+存活过滤的**结果**，`provinces: 260` 是**目标**，两者不等是正常行为，但容易被误读 |

### 设计层面的观察（不是 bug）

1. **投资刹车不够狠**。`investCost` 是二次的、`MAX_LEVEL` 是硬顶，长跑证明硬顶而非成本才是真正的终点：1200 tick 后 97% 的省撞顶。想让世界别冻结，要么让 `MAX_LEVEL` 随科技/时代解锁，要么让满级省有别的花钱处（基建、军备、税制）。
2. **剩余区分度只有"不满"**。1800 tick 后不满 p99=0.80、p50=0，是全模型唯一还带正反馈的量——恰恰是 README 说的"唯一正反馈"。这既是设计成功的证据，也说明其他指标缺少二阶动态。
3. `render.js:295 / 745` 的 `selectedCountry` 是**死变量**（setCountry 无人调用），地图从不按"当前国家"做高亮。
4. `buildCountryBorders` 在启动时对每条边、每 4px 采样点都做一次全站点最近邻（O(省 × 边 × 采样 × 省)），是本项目最重的一次性计算；省数上千时需要空间网格。
5. `pick()` 每次 mousemove 线性扫全部省份（214 个可接受，只是 O(P)）。

---

## 四、下一步该怎么走（按性价比排序）

1. **修面板数字 bug**（10 分钟，用户可见）。
2. **做玩家操作**：建筑队列 + 税率滑块。README 自己也判"这是最该做的下一步"。注意 `sim.js` 已经预留了 `TAX_RATE` 与 `treasury`（国库现在只进不出，是天然的消费口）。
3. **决定"100 年之后"的设计**：现在模拟必然走向冻结。建议把 `MAX_LEVEL` 改成按年代解锁的技术阶梯，让"撞顶"变成"换挡"。
4. **补 render/ui 的回归测试**：至少要有一个 headless DOM harness 或 CDP 探针（本次用的 `/tmp/vt-probe.mjs`、`/tmp/vt-fps.mjs` 可直接改造成 `test/browser.js`），否则同类 bug 还会继续出现。
5. **给 `sim.js` 加长跑断言**（1500 tick 后人口/GDP 不应完全冻结），把"相变点"变成受监控的指标。
6. **接 git**。3500 行、13 条踩坑记录的项目没有版本控制，任何一次实验性改动都无法回退——这是当前最大的工程风险。

---

## 五、复现本次结论的命令

```bash
# 1) 自带回归
cd vic-toy && node test/check.js            # 44 项全绿
# 2) 地图形状 / 种子排名
node test/preview.js 8888 104
node test/seed.js
# 3) 浏览器实测（需本地服务器，自动化浏览器打不开 file://）
python3 -m http.server 8811 --bind 127.0.0.1
#   然后用 Edge + CDP 驱动（本次用的探针脚本在 /tmp/vt-probe.mjs、/tmp/vt-fps.mjs）
# 4) 100 年长跑（本次新写）
node /tmp/vt-longrun.js
# 5) 国家连通性 + 国名锚点（本次新写）
node /tmp/vt-country.js 8888 104
```

> 本次研究未修改 `vic-toy` 下任何文件。
